# SUPER-MAN-AI (AI ENGINEER) - Python backend dependencies.
# Install:  python -m venv .venv && .venv/Scripts/pip install -r requirements.txt
pydantic>=2.7
pint>=0.24
fastapi>=0.111
uvicorn[standard]>=0.30
pyyaml>=6.0
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
numpy>=1.26
sympy>=1.12
structlog>=24.1
playwright>=1.45

# Frontend (separate): cd frontend && npm install
